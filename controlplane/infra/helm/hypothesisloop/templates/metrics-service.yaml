apiVersion: v1
kind: Service
metadata:
  name: metrics-service
  namespace: {{ include "hypothesisloop.namespace" . }}
spec:
  selector:
    app: hypothesisloop-metrics-service
  ports:
    - port: {{ .Values.metricsService.port }}
      targetPort: {{ .Values.metricsService.port }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hypothesisloop-metrics-service
  namespace: {{ include "hypothesisloop.namespace" . }}
spec:
  replicas: {{ .Values.metricsService.replicas }}
  selector:
    matchLabels:
      app: hypothesisloop-metrics-service
  template:
    metadata:
      labels:
        app: hypothesisloop-metrics-service
    spec:
      {{- if not .Values.postgres.external.enabled }}
      initContainers:
        - name: wait-for-postgres
          image: {{ .Values.postgres.image }}
          command: ["sh", "-c", "until pg_isready -h hypothesisloop-postgres -U {{ .Values.postgres.user }}; do sleep 2; done"]
      {{- end }}
      containers:
        - name: metrics-service
          image: {{ include "hypothesisloop.image" (dict "name" "metrics-service" "root" $) }}
          imagePullPolicy: {{ .Values.images.pullPolicy }}
          env:
            - name: METRIC_CONTROLLER_PORT
              value: {{ .Values.metricsService.port | quote }}
            - name: HYPOTHESISLOOP_CONFIG
              value: /settings/hypothesisloop.yaml
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "hypothesisloop.databaseSecretName" . }}
                  key: {{ include "hypothesisloop.databaseSecretKey" . }}
          ports:
            - containerPort: {{ .Values.metricsService.port }}
          volumeMounts:
            - name: config
              mountPath: /settings
              readOnly: true
          resources:
            {{- toYaml .Values.metricsService.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.metricsService.port }}
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: config
          configMap:
            name: hypothesisloop-config
