# Local-only files (secrets, machine-specific notes) -- suffix with .local
*.local
*.local.*

# Go
/bin/
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
coverage.txt
coverage.html

# Compiled service binaries in repo root
/control-service
/metrics-service
/node-agent
/cluster-agent

# Go workspace
go.work
go.work.sum

# Build output
dist/

# Next.js
controlplane/ui/.next/
controlplane/ui/out/
controlplane/ui/node_modules/
controlplane/ui/.env.local
controlplane/ui/.env.development.local
controlplane/ui/.env.test.local
controlplane/ui/.env.production.local
controlplane/ui/npm-debug.log*
controlplane/ui/yarn-debug.log*
controlplane/ui/yarn-error.log*
controlplane/ui/tsconfig.tsbuildinfo

# Python
**/__pycache__/
**/*.pyc
**/.venv/
**/venv/
**/wandb/
**/outputs/
**/checkpoints/
*.pt
*.pth
*.ckpt

# k3s
localdev/**/kubeconfig
*.kubeconfig
*.pem
*.key
*.crt

# Docker / secrets
.env
.env.*
!.env.example
docker-compose.override.yml

# Editor
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# OS
Thumbs.db

# Logs
*.log
logs/

# Secrets / credentials
*.secret
secrets/

# Go module vendor directory (not tracked)
vendor/
# Claude Code session state (worktrees, local agent scratch)
.claude/

# Experiment working clones: each is its own upstream git repo (source, datasets,
# vendored toolchains) cloned in place, not content of this repo. Only the coordinator's
# own experiment.md/seed/ files under agents/coordinator/experiments/<name>/ are tracked.
agents/coordinator/experiments/smri-fm/
agents/coordinator/experiments/smri-fm-notes/
